-- globals.lua

-- Markdown preview plugin
vim.g.mkdp_auto_start = 0   -- do NOT start automatically
vim.g.mkdp_auto_close = 1   -- close preview when closing buffer
vim.g.mkdp_refresh_slow = 0 -- fast refresh
vim.g.mkdp_open_to_the_world = 0 -- only localhost
vim.g.mkdp_browser = ""     -- default system browser
--vim.g.mkdp_port = "8080"    -- default port
